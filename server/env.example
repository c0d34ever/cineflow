# Backend Environment Variables
# Copy this file to .env and update with your actual values

# Server Configuration
NODE_ENV=production
PORT=5000

# Database Configuration (Your Production Database)
DB_HOST=162.241.86.188
DB_PORT=3306
DB_USER=youtigyk_cineflow
DB_PASSWORD=Sun12day46fun
DB_NAME=youtigyk_cineflow

# Gemini API Key (Required for AI features)
GEMINI_API_KEY=AIzaSyA5W1AaxVAyCgEjvs3_Ai3mrg31FZllhPI

# CORS Configuration (optional - comma-separated)
CORS_ORIGIN=http://localhost:3000,http://localhost:80,http://209.182.232.185:8081,https://209.182.232.185:8081,http://youtilitybox.com,https://youtilitybox.com,http://www.youtilitybox.com,https://www.youtilitybox.com,https://cineflow.youtilitybox.com,http://cineflow.youtilitybox.com

# JWT Configuration (for authentication)
JWT_SECRET=your-secret-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=24h

# Admin Microservice Configuration
ADMIN_PORT=5001
ADMIN_CORS_ORIGIN=http://localhost:3000,http://localhost:80,http://209.182.232.185:8081,https://209.182.232.185:8081,http://youtilitybox.com,https://youtilitybox.com,http://www.youtilitybox.com,https://www.youtilitybox.com,https://cineflow.youtilitybox.com,http://cineflow.youtilitybox.com

# Default Admin User (created on first seed)
ADMIN_EMAIL=admin@cineflow.ai
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Database Seeding
SEED_DATABASE=true

# ImageKit Configuration (Optional - uses defaults if not set)
IMAGEKIT_PUBLIC_KEY=public_bH2QtMc9E/U08Hzj2PN7tkoTD7o=
IMAGEKIT_PRIVATE_KEY=private_+PmUqowiTKhJc1E0hSgwpMtF6J8=
IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/hkorgalju

# Encryption Key (REQUIRED for email password encryption)
# Must be at least 32 characters long
# Generate a secure random key: openssl rand -base64 32
ENCRYPTION_KEY=your-encryption-key-minimum-32-characters-long-change-in-production

# Encryption Key (REQUIRED for email password encryption)
# Must be at least 32 characters long
# Generate a secure random key: openssl rand -base64 32
ENCRYPTION_KEY=your-encryption-key-minimum-32-characters-long

# SMTP Email Configuration (OPTIONAL - can be managed via Admin Dashboard)
# These will be automatically seeded to database on first startup if provided
# You can also configure them directly in Admin Dashboard → Email Templates → Edit Settings
# Supports both SMTP_* and MAIL_* variable names (Laravel-style)

# Standard naming (SMTP_*):
SMTP_HOST=youtilitybox.com
SMTP_PORT=587
SMTP_USER=info@youtilitybox.com
SMTP_PASSWORD=Sun12day46fun
SMTP_FROM=info@youtilitybox.com
SMTP_ENCRYPTION=ssl

# Alternative naming (MAIL_*) - also supported:
MAIL_MAILER=smtp
MAIL_HOST=youtilitybox.com
MAIL_PORT=587
MAIL_USERNAME=info@youtilitybox.com
MAIL_PASSWORD=Sun12day46fun
MAIL_ENCRYPTION=ssl
MAIL_FROM_ADDRESS=info@youtilitybox.com

# Frontend URL for email links (OPTIONAL - can be set in Admin Dashboard)
# Used for constructing email links (password reset, verification, etc.)
FRONTEND_URL=https://cineflow.youtilitybox.com

